# Single-package project; declare the root package explicitly so both
# pnpm 11+ and changesets find it. Empty list ('packages: []') makes
# changesets fail with "No packages found".
packages:
  - '.'

allowBuilds:
  esbuild: set this to true or false

onlyBuiltDependencies:
  - esbuild
  - protobufjs
